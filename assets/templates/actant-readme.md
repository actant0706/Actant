# Actant Run

This directory contains a persisted Actant run. Machine state is stored in JSON files. Markdown files are human-readable artifacts and do not satisfy gates by file existence alone.

Use `python actant/scripts/actantctl.py validate` from the project root to validate the run structure.
